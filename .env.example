# ============================================================================
# BareMetalHost Generator Operator - Environment Configuration
# ============================================================================
# Copy this file to .env and fill in your actual values
# DO NOT commit .env file to version control!

# ============================================================================
# Logging Configuration
# ============================================================================
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# ============================================================================
# Buffer Management
# ============================================================================
# Maximum number of available (non-provisioned) BareMetalHosts in the cluster
MAX_AVAILABLE_SERVERS=20

# How often (in seconds) to check if buffered servers can be released
BUFFER_CHECK_INTERVAL=30

# ============================================================================
# HP OneView Configuration
# ============================================================================
# Comment out or leave empty if not using HP OneView
ONEVIEW_IP=
ONEVIEW_USERNAME=administrator
ONEVIEW_PASSWORD=

# ============================================================================
# Cisco UCS Central Configuration
# ============================================================================
# Comment out or leave empty if not using Cisco UCS
UCS_CENTRAL_IP=
UCS_CENTRAL_USERNAME=admin
UCS_CENTRAL_PASSWORD=

# UCS Manager credentials (used to connect to individual UCS Managers)
UCS_MANAGER_USERNAME=admin
UCS_MANAGER_PASSWORD=

# ============================================================================
# Dell OpenManage Enterprise (OME) Configuration
# ============================================================================
# Comment out or leave empty if not using Dell OME
OME_IP=
OME_USERNAME=admin
OME_PASSWORD=

# ============================================================================
# Default IPMI/BMC Credentials (used for ALL servers)
# ============================================================================
# These credentials are used in the BMC secret for BareMetalHost resources
# Used by Metal3/Ironic to control physical servers (power, boot, etc)
DEFAULT_IPMI_USERNAME=administrator
DEFAULT_IPMI_PASSWORD=

# ============================================================================
# IMPORTANT NOTES
# ============================================================================
#
# 1. Management System Credentials vs BMC Credentials:
#    - Management System Credentials (ONEVIEW_*, UCS_*, OME_*):
#      Used by the OPERATOR to login and query server MAC/IP information
#
#    - BMC Credentials (DEFAULT_IPMI_USERNAME/PASSWORD):
#      Used by the BAREMETALHOST to control the physical servers (power, boot, etc)
#      These are stored in Kubernetes secrets and used by Metal3/Ironic
#
# 2. You must configure at least ONE vendor's management system credentials
#    AND the DEFAULT_IPMI credentials.
#
# 3. The same BMC credentials are used for all servers regardless of vendor.
#
# 4. Credentials are stored as PLAIN TEXT in environment variables.
#    The operator will base64-encode them when creating Kubernetes secrets.
#
# 5. If BMC credentials are not set, the operator will fail with a clear
#    error message when trying to create a BareMetalHost.
#
